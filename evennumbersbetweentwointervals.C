#include<stdio.h>
#include<conio.h>
int main()
{
int k,n;
printf("Enter the interval to find the even numbers between them \n");
scanf("%d",k);
printf("print all the even numbers between them \n");
for(k=1;k<n;k++)
{
if(k%2=0)
{
print("%d",k);
}
}
return 0;
}
