#include<stdio.h>
#include<conio.h>
int(int n1,int n2)
{
int base,pow,result;
printf("Enter the base value");
scanf("%d",&base);
printf("Enter the value of power");
scanf(:%d",&pow);
result=pow*(base,pow);
printf("%d%d=%d",the result is:\n");
}
int pow(int base,int pow)
{
result=base*pow(base,pow-1);
{
if(result!=0)
return 0;
else
return 1;
}
