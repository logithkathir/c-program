#include<stdio.h>
#include<conio.h>
int main()
{
int r,num,temp,sum=0;
printf("Enter the number \n");
scanf("%d",&num);
temp=num;
while(temp!=0)
{
r==num%10;
num=num/10;
sum=sum+(r*r*r);
{
if(sum==temp)
printf("Enter the given number is a Amstrong number \n");
else
printf("Enter the given number is not an Amstrong number \n");
}
}
}
