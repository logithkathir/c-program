#include<stdio.h>
#include<conio.h>
#include<math.h>
int main()
{
int  k,num,sum;
printf("Enter the number \n");
scanf("%d",&num);
for(i=1;i<=num;i++)
{
sum=sum+i;
}
printf("the sum of the natural numbers is:=%d,num,sum");
getch();
}
